name: adisyon_server
description: Adisyon REST API Server
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  postgres: ^3.5.5
  args: ^2.5.0

dev_dependencies:
  lints: ^4.0.0
